info:
  name: tga
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}?url=fnaf.tga"
  params:
    - name: url
      value: fnaf.tga
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
