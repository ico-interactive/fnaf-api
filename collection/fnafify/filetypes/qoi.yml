info:
  name: qoi
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}?url=fnaf.qoi"
  params:
    - name: url
      value: fnaf.qoi
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
