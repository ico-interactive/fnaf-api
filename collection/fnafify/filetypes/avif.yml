info:
  name: avif
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}?url=fnaf.avif"
  params:
    - name: url
      value: fnaf.avif
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
