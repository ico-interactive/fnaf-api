info:
  name: ico
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}?url=fnaf.ico"
  params:
    - name: url
      value: fnaf.ico
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
