info:
  name: pbm
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}?url=fnaf.pbm"
  params:
    - name: url
      value: fnaf.pbm
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
