info:
  name: tiff
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}?url=fnaf.tiff"
  params:
    - name: url
      value: fnaf.tiff
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
