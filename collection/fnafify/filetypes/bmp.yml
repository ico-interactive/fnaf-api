info:
  name: bmp
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}?url=fnaf.bmp"
  params:
    - name: url
      value: fnaf.bmp
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
