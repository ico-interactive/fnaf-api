info:
  name: gif
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}?url=fnaf.gif"
  params:
    - name: url
      value: fnaf.gif
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
