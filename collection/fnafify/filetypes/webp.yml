info:
  name: webp
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}?url=fnaf.webp"
  params:
    - name: url
      value: fnaf.webp
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
