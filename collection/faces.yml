info:
  name: faces
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}faces"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://127.0.0.1:9638/

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
